<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#0D0D0D">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
  <div class="app-container">
    <!-- Main Content -->
    <main class="main-content reader-content">
      <!-- Reader Header -->
      <div class="reader-header">
        <a href="/comic/<%= comic.slug %>" class="btn-back">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M19 12H5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M12 19L5 12L12 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
        <div class="reader-title">
          <h1><%= comic.title %></h1>
          <p>Chapter <%= chapterNum %></p>
        </div>
      </div>

      <!-- Reader Images (Placeholder) -->
      <div class="reader-images">
        <div class="reader-placeholder">
          <svg width="100" height="100" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M19 3H5C3.89543 3 3 3.89543 3 5V19C3 20.1046 3.89543 21 5 21H19C20.1046 21 21 20.1046 21 19V5C21 3.89543 20.1046 3 19 3Z" stroke="#F7C843" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M8.5 10C9.32843 10 10 9.32843 10 8.5C10 7.67157 9.32843 7 8.5 7C7.67157 7 7 7.67157 7 8.5C7 9.32843 7.67157 10 8.5 10Z" stroke="#F7C843" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M21 15L16 10L5 21" stroke="#F7C843" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <p>Chapter Reader Placeholder</p>
          <p class="placeholder-note">Dalam implementasi sebenarnya, gambar-gambar chapter akan ditampilkan di sini secara vertikal untuk dibaca dengan scroll.</p>
        </div>
      </div>

      <!-- Reader Navigation -->
      <div class="reader-navigation">
        <%
          const currentIndex = comic.chapters.findIndex(ch => ch.number === chapterNum);
          const prevChapter = comic.chapters[currentIndex + 1];
          const nextChapter = comic.chapters[currentIndex - 1];
        %>

        <% if (prevChapter) { %>
          <a href="/comic/<%= comic.slug %>/chapter/<%= prevChapter.number %>" class="btn-nav-chapter">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M15 18L9 12L15 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            Chapter Sebelumnya
          </a>
        <% } else { %>
          <div class="btn-nav-chapter disabled">Tidak ada chapter sebelumnya</div>
        <% } %>

        <% if (nextChapter) { %>
          <a href="/comic/<%= comic.slug %>/chapter/<%= nextChapter.number %>" class="btn-nav-chapter">
            Chapter Selanjutnya
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M9 18L15 12L9 6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        <% } else { %>
          <div class="btn-nav-chapter disabled">Tidak ada chapter selanjutnya</div>
        <% } %>
      </div>
    </main>
  </div>

  <script src="/js/main.js"></script>
</body>
</html>
