<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#0D0D0D">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>
  <div class="app-container">
    <!-- Main Content -->
    <main class="main-content">
      <!-- Page Header -->
      <div class="page-header">
        <h1>Favorit Saya</h1>
      </div>

      <!-- Favorites List -->
      <div class="favorites-list">
        <% if (favorites.length > 0) { %>
          <% favorites.forEach(comic => { %>
            <div class="favorite-item">
              <a href="/comic/<%= comic.slug %>" class="favorite-link">
                <div class="favorite-cover">
                  <img src="<%= comic.cover %>" alt="<%= comic.title %>">
                </div>
                <div class="favorite-info">
                  <h3 class="favorite-title"><%= comic.title %></h3>
                  <div class="favorite-rating">
                    <span class="star">⭐</span>
                    <span><%= comic.rating.toFixed(1) %></span>
                  </div>
                  <p class="favorite-description"><%= comic.description.substring(0, 100) %>...</p>
                </div>
              </a>
              <button class="btn-remove-favorite" data-comic-id="<%= comic.id %>">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  <path d="M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </button>
            </div>
          <% }); %>
        <% } else { %>
          <div class="empty-state">
            <img src="https://i.imgur.com/XqQXQ0Y.png" alt="Empty" class="empty-image">
            <p class="empty-text">Belum ada komik favorit</p>
            <a href="/" class="btn-primary">Cari Komik</a>
          </div>
        <% } %>
      </div>
    </main>

    <!-- Bottom Navigation -->
    <%- include('components/bottom_nav') %>
  </div>

  <script src="/js/main.js"></script>
</body>
</html>
